install:
  - sudo apt-get update
  - sudo apt-get install texlive-full
script:
  - pdflatex -synctex=1 -interaction=nonstopmode tfg.tex
  - bibtex tfg.aux
  - pdflatex -synctex=1 -interaction=nonstopmode tfg.tex 
  - pdflatex -synctex=1 -interaction=nonstopmode tfg.tex
